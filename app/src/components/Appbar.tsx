import styled from 'styled-components/native';
import colors from '../styles/colors';
import { Appbar as PaperAppbar} from 'react-native-paper';

const Appbar = styled(PaperAppbar)`
  font-size: 26px;
  color: red;
`;

export default Appbar;
