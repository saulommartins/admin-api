import styled from 'styled-components/native';
import {
  KeyboardAvoidingView as KeyboardAvoidingViewStyled,
} from 'react-native';

const KeyboardAvoidingView = styled(KeyboardAvoidingViewStyled)`
  flex: 1;
`;

export default KeyboardAvoidingView;
