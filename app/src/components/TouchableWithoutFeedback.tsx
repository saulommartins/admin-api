import styled from 'styled-components/native';
import {
  TouchableWithoutFeedback as TouchableWithoutFeedbackStyled,
} from 'react-native';

const TouchableWithoutFeedback = styled(TouchableWithoutFeedbackStyled)`
  flex: 1;
`;

export default TouchableWithoutFeedback;
